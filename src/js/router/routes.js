import Dashboard from '../pages/Dashboard';
import Settings from '../pages/Settings';

const routes = [
    {
        path: '/',
        component: Dashboard,
    },
    {
        path: '/settings',
        component: Settings,
    },
];

export default routes;